apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: obs-api-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: obs-api
                port:
                  number: 3000
